export type Meetup = {
  address: string;
  description: string;
  id: string;
  image: string;
  title: string;
};

export type MeetupsArray = Meetup[];

export type NewMeetupData = {
  address: string;
  description: string;
  image: string;
  title: string;
};
